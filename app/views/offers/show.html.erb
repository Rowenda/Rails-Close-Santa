<h2>Show</h2>
<p><%= @offer.title %></p>
<p><%= @offer.offer_description %></p>
<p><%= @offer.santa_description %></p>
<p><%= @offer.availability %></p>
<p><%= @offer.location %></p>
<p><%= @offer.price_per_hour %></p>
<%= link_to "Back to offers", offers_path %>
<%= link_to 'reservation', new_offer_reservation_path(@offer.id) %>

<%# a voir pas accesible pour le moment %>
<%# lien delete %>
<%# <%= link_to "Delete #{@offer.title}",
            destroy_offer_path(@offer),
            method: :delete,
            data: { confirm: "Are you sure?" }manquue fermetuture chevron percent %>


<% if current_user == @offer.user %>
<%= link_to "Delete #{@offer.title}",
            offer_path(@offer),
            method: :delete,
            data: { confirm: "Are you sure?" } %>
<% end %>
